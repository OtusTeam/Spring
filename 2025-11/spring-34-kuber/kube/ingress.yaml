---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rest-hello
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rest-hello
                port:
                  number: 80
