spring:
  datasource:
    url: jdbc:h2:mem:testdb
    #    username: postgres
    #    password: admin
    #    driver-class-name: org.postgresql.Driver
    initialization-mode: always
  #    schema: schema.sql
  #    data: data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

logging:
  level:
    ROOT: ERROR